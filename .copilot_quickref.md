# 🚀 GitHub Copilot Quick Reference
# Never Make The Same Mistake Twice

## ⚠️ Critical Rules (Violate At Your Own Risk)

### 1. Terminal Isolation
- **Background services** → `nohup command > log 2>&1 &`
- **Interactive commands** → Separate terminal sessions
- **Never mix**: Server terminal + client commands = 💥 SHUTDOWN

### 2. Pre-Command Validation (MANDATORY)
```bash
# ALWAYS validate before running commands
./Tools/Automation/validate_command.sh "your command" true/false

# ✅ SAFE Examples:
./Tools/Automation/validate_command.sh "curl http://localhost:5005/status" false
./Tools/Automation/validate_command.sh "ps aux | grep mcp" false

# ⚠️ FLAGGED Examples (review before proceeding):
./Tools/Automation/validate_command.sh "curl http://api.github.com" false
./Tools/Automation/validate_command.sh "python3 setup.py install" false
```

**Smart Validation:**
- 🟢 **Green**: Safe operations (local status checks)
- 🟡 **Yellow**: Review needed (external calls, missing directories)
- 🔴 **Red**: Blocked (port conflicts, dangerous operations)

### 3. Process Management
- Check ports: `lsof -i :port`
- Kill conflicts: `lsof -ti:port | xargs kill -9`
- Verify running: `ps aux | grep process_name`

## ✅ Safe Command Patterns

### Starting Services
```bash
# ✅ CORRECT: Background service startup
./Tools/Automation/validate_command.sh "python3 mcp_server.py" true && \
nohup python3 mcp_server.py > mcp_server.log 2>&1 &

# ❌ WRONG: Will get interrupted
python3 mcp_server.py  # Then run curl in same terminal
```

### Testing Services
```bash
# ✅ CORRECT: Separate terminal for testing
curl -s http://localhost:5005/status

# ❌ WRONG: Same terminal as running service
# (Causes ^C shutdown)
```

## 🔍 Error Recovery

When you see `^C` in terminal output:
1. Service was interrupted by another command
2. Restart with `nohup` in new terminal
3. Use separate terminal for testing

## 📊 Success Metrics Target
- Commands without interruption: 100%
- Background services staying alive: 100%
- Validation script usage: 100%</content>
<parameter name="filePath">/Users/danielstevens/Desktop/Quantum-workspace/.copilot_quickref.md